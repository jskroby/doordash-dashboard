npm install -g firebase-tools
firebase login
firebase loginfirebase init hosting
firebase init hosting
firebase init hostingpython3 -m http.server 80
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy --only hosting
firebase init hosting
firebase deploy --only hosting
cat ~/.bash_history
ls -l public/
cat public/index.html
cd ~/public
ls -l
ls
cd public
ls
cd ~/public
git init
git add .
git commit -m "Initial commit of DoorDash dashboard"
git push -u origin main
git init && git checkout -b main && git add . && git commit -m "Initial commit" && git remote add origin https://github.com/your-username/your-repo.git && git push -u origin main
cd ~/public
git remote set-url origin https://jskroby@github.com/jskroby/doordash-dashboard.git
git push -u origin main
CD
cd
mkdir -p .github/workflows
nano .github/workflows/firebase-deploy.yml
mkdir -p .github/workflows
nano .github/workflows/firebase-deploy.yml
cd workflows
ls
nano firebase.json
cd public
ls
cd public
cd
ls
mkdir -p .github/workflows
nano .github/workflows/firebase-deploy.yml
ls
cd public
ls
ls -a ~/.github/workflows
cd ~/.github/workflows
ls
cd
cd ~
git add .github/workflows/firebase-deploy.yml
git commit -m "Add Firebase deploy workflow"
git push
cd public 
cd ~
git add .github/workflows/firebase-deploy.yml
git commit -m "Add Firebase deploy workflow"
git push
cd public
cd ~
git add .github/workflows/firebase-deploy.yml
git commit -m "Add Firebase deploy workflow"
git push
cd ~/public  # Go to the Git repo
mv ~/.github .  # Move the .github folder into the repo
git add .github/workflows/firebase-deploy.yml
git commit -m "Add Firebase deploy workflow"
git push
git pull --rebase origin main
git push
cd public
ls
git pull --rebase origin main
git push
# Start your local server on port 9005
python3 -m http.server 9005
python3 -m http.server 9006
firebase login:ci
firebase login --no-localhost
firebase login:ci
cd ~/public
firebase deploy --only hosting
firebase login:ci
firebase login --no-localhost
firebase deploy --only hosting
firebase login:ci --no-localhost
cd ~
mv public/.git .
cd ~
mv public/.git .
